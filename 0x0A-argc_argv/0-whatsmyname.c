#include <stdio.h>

/**
 * main - prints a program's name
 * @argc: argument count.
 * @argv: argument vector.
 *
 * Return: returns 0.
 */

int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
