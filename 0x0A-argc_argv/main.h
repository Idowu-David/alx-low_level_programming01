#ifndef MAIN_H
#define MAIN_H

/* Standard Library */
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdlib.h>

/* Function Prototype */
int _putchar(char c);
int isnumber(const char *str);
int isInteger(const char *num);
int change(int num);
#endif
