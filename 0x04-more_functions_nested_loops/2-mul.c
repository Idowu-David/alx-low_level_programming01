#include "main.h"
/**
 * mul - multiplies two integers.
 * @a: first integer.
 * @b: second integer.
 *
 * Return: Returns the result of multiplication
 * of two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
